# Helper Dockerfile (optional): produces the MOS12 source bundle by running build_mos12.sh.
#
# IMPORTANT: do NOT use this image to build an installable MOS 12 RPM. Building on ALT Sisyphus
# yields binaries requiring newer glibc/rpm than MOS 12 provides.

FROM alt:sisyphus

WORKDIR /work
COPY . /work

CMD ["bash", "-lc", "./build_mos12.sh"]